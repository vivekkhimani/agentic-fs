---
type: index
title: example-note
timestamp: 2026-06-16
---

# example-note

One-line catalog of this directory's knowledge pages, grouped by `type`.

## concept

- [[Vector databases vs. agentic filesystem navigation]] — Why compounding markdown wikis beat
  re-retrieval for long-lived knowledge.

---
<!-- Notes for the conformance suite:
     - The index is generated from each page's OKF frontmatter (title + description + type),
       not from body content — so it is deterministic and diffable.
     - Round-trip lossy point to document: native `created` + `updated` collapse to a single OKF
       `timestamp`. A conformant ingest must NOT fabricate a `created` it cannot recover. -->
